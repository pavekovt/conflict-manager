ktor:
    application:
        modules:
            - me.pavekovt.ApplicationKt.module
    deployment:
        port: 8080
postgres:
  url: "jdbc:postgresql://localhost/default"
  user: username
  password: passwordjwt
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"


jwt:
  secret: test
  issuer: confresolv
  audience: all
  realm: main