ktor:
    application:
        modules:
            - me.pavekovt.ApplicationKt.module
    deployment:
        port: 8080
postgres:
  url: "jdbc:postgresql://localhost/conflict_manager"
  user: dev_user
  password: dev_password


jwt:
  secret: test
  issuer: confresolv
  audience: all
  realm: main

ai:
  # AI Provider: "mock" for development, "claude" for production
  provider: claude
  # Claude API key (also can be set via CLAUDE_API_KEY environment variable)
  # apiKey: your-api-key-here
  # AI model to use (default: claude-3-5-sonnet-20241022)
  # model: claude-3-5-sonnet-20241022